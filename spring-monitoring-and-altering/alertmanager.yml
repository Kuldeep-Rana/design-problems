global:
  resolve_timeout: 5m

route:
  receiver: 'slack-notify'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'slack-notify'
    webhook_configs:
      - url: 'http://your-slack-webhook-or-proxy'  # replace with your webhook or use email_config
